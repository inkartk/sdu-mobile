import 'package:flutter/material.dart';

class MintDivider extends StatelessWidget {
  const MintDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 6,
      width: double.infinity,
      color: const Color(0xFFD0E6DC),
    );
  }
}
